# Generated by apps\forge-vscode\scripts\apply-overlay.ps1 - do not hand-edit.
#
# The fork's root .gitignore excludes 'extensions/**/dist/' and a bare 'dist'.
# This folder IS the overlay output and has to be tracked on the forge branch,
# so re-include the prebuilt bundle here, where the deeper file wins.
!/dist/
!/dist/**
